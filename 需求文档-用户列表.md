# 用户列表 · 字段扩展需求

| 项目 | 内容 |
| --- | --- |
| 需求名称 | 业务数据 - 用户管理 - 用户列表 · 字段扩展 |
| 提出方 | 财务 |
| 关联需求 | [需求文档-经营者收入报送表](需求文档-经营者收入报送表.md) |
| 落地位置 | 业务数据 → 用户管理 → 用户列表 |
| 文档版本 | v0.1 |
| 文档日期 | 2026-05-18 |

---

## 一、背景

为支撑《经营者与从业人员收入信息报送表》的**季度**报送与核对，需要在用户列表中直接看到认证主体相关字段，并在导出时附带财务人工核对所需的联系人、地址信息。本需求只改造既有用户列表，不新增页面。

---

## 二、列表展示列

### 2.1 新增列

| 列号 | 列名 | 取值规则 |
| --- | --- | --- |
| — | 认证类型 | 取实名认证主体类型，枚举值：个人 / 企业 / 个体工商户；**未认证用户展示为 `-`** |
| — | 统一代码 | 取「统一社会信用代码」或「组织机构代码」（即纳税人识别号），对应字段 `creditCode`；若证件类型不是统一代码或组代，展示为 `-` |

### 2.2 位置

两列均插入到 **「电话」列之后、「来源」列之前**，顺序为：电话 → 认证类型 → 统一代码 → 来源。

### 2.3 其他列保持不变

用户名、姓名、电话、来源、注册日期、可用余额、待确认收款、保证金、总金额、操作 等既有列展示不变。

---

## 三、导出表格

在现有导出字段基础上新增以下列：

| 列名 | 取值规则 |
| --- | --- |
| 认证类型 | 同列表 2.1 |
| 统一代码 | 同列表 2.1 |
| 联系人 | 取用户表「经办人姓名」；无值取「法人姓名」；仍无值取「用户列表 · 姓名」字段 |
| 公司地址 | 优先取「营业执照地址」；如无则取「门店信息 · 门店地址」；两者均无则留空（人工补录） |

---

## 四、字段口径说明

1. **认证类型**：个人 / 企业 / 个体工商户；未认证用户展示为 `-`。
2. **统一代码**：统一社会信用代码和组织机构代码（即纳税人识别号）；如证件类型不是统一代码或组代，展示为 `-`。
3. **联系人**：取用户表「经办人姓名」字段，有值即取；无值取「法人姓名」；两者均无值则取「用户列表 · 姓名」字段。
4. **公司地址**：优先取「营业执照地址」；如无则取「门店信息 · 门店地址」；两者均无则留空（人工补录）。

> **联系人、公司地址**仅作为财务/税局沟通时的其他报表上送字段，由财务人员人工整理。

---

## 五、待确认事项

无。
